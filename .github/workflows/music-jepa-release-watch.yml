name: Music-JEPA release watch

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Check sources without committing state or sending email'
        required: false
        default: false
        type: boolean
      test_email:
        description: 'Only send a test email using the existing SMTP secrets'
        required: false
        default: false
        type: boolean
  schedule:
    # Daily 07:37 UTC = 09:37 CEST / 08:37 CET; Actions may start late.
    # The collector runs daily; third-party emails have a persisted 7-day gate.
    - cron: '37 7 * * *'
  push:
    branches: [main]
    paths:
      - '.github/workflows/music-jepa-release-watch.yml'
      - 'music_jepa_*.py'
      - 'test_music_jepa*.py'

permissions:
  contents: write

concurrency:
  group: music-jepa-release-watch
  cancel-in-progress: false

jobs:
  watch:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Offline regression tests
        run: python -m unittest discover -p 'test_music_jepa*.py' -v

      - name: Send test email
        if: inputs.test_email == true && inputs.dry_run != true
        env:
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          ALERT_EMAIL_TO: ${{ secrets.ALERT_EMAIL_TO }}
          ALERT_EMAIL_FROM: ${{ secrets.ALERT_EMAIL_FROM }}
        run: |
          printf 'Test des Music-JEPA-Monitors. Tagesalarme und Drittanbieter-Wochensammlung verwenden diese SMTP-Konfiguration.\n' > "$RUNNER_TEMP/music-jepa-test.txt"
          python send_alert.py --subject "[Web-Monitor] Music-JEPA: Test" --body-file "$RUNNER_TEMP/music-jepa-test.txt"

      - name: Run watcher and prepare separate deliveries
        id: watch
        if: inputs.test_email != true
        env:
          GITHUB_TOKEN: ${{ github.token }}
          DRY_RUN: ${{ inputs.dry_run }}
        run: |
          args=()
          if [ "$DRY_RUN" = "true" ]; then
            args+=(--dry-run)
          fi
          python music_jepa_delivery.py "${args[@]}" \
            --alert-file "$RUNNER_TEMP/music-jepa-daily.txt" \
            --weekly-alert-file "$RUNNER_TEMP/music-jepa-weekly.txt"

      - name: Send daily source or author alert
        if: steps.watch.outputs.daily_count > 0 && inputs.dry_run != true
        env:
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          ALERT_EMAIL_TO: ${{ secrets.ALERT_EMAIL_TO }}
          ALERT_EMAIL_FROM: ${{ secrets.ALERT_EMAIL_FROM }}
          ALERT_SUBJECT: ${{ steps.watch.outputs.daily_subject }}
        run: >
          python send_alert.py
          --subject "$ALERT_SUBJECT"
          --body-file "$RUNNER_TEMP/music-jepa-daily.txt"

      - name: Send weekly third-party digest only when due and nonempty
        if: steps.watch.outputs.weekly_count > 0 && inputs.dry_run != true
        env:
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          ALERT_EMAIL_TO: ${{ secrets.ALERT_EMAIL_TO }}
          ALERT_EMAIL_FROM: ${{ secrets.ALERT_EMAIL_FROM }}
          ALERT_SUBJECT: ${{ steps.watch.outputs.weekly_subject }}
        run: >
          python send_alert.py
          --subject "$ALERT_SUBJECT"
          --body-file "$RUNNER_TEMP/music-jepa-weekly.txt"

      # After BOTH SMTP steps. An unsent alert/digest must not be consumed.
      # On quiet days this still saves queued third-party discoveries.
      - name: Commit state and report
        if: inputs.dry_run != true && inputs.test_email != true
        run: |
          git config user.name "page-watch-bot"
          git config user.email "page-watch-bot@users.noreply.github.com"
          git add -- seen_music_jepa.json reports/music_jepa_latest.md
          if git diff --staged --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "Update Music-JEPA release watch state and report"
          for attempt in 1 2 3; do
            if git push origin HEAD:main; then
              exit 0
            fi
            git pull --rebase origin main
          done
          git push origin HEAD:main

      - name: Mark incomplete checks as failed
        if: steps.watch.outputs.error_count > 0
        run: |
          echo "Some sources could not be checked. See the job summary/report; this is not a no-news result."
          exit 1
